# 白名单式：默认忽略一切，只放行自定义文件。
# 这样 plum 更新词库（44M 的 cn_dicts、74M 的 build）不会污染仓库。

*
!.gitignore
!README.md

# 三个自定义补丁
!*.custom.yaml

# lua 目录：先放行目录本身，git 才能进去看，再放行里面的自定义脚本
!lua/
lua/*
!lua/radical_sort_filter.lua
!lua/char_freq.lua

# 故意不跟踪的：
#   build/          编译产物，重新部署会生成
#   cn_dicts/       雾凇词库，plum 管
#   *.userdb/       个人学习词库，是 LevelDB，跨机器直接拷可能不被识别，
#                   走鼠鬚管菜单的「同步用户资料」导出成文本更可靠
#   sync/           同步导出目录，含 installation_id，每台机器不同
#   installation.yaml  含本机 installation_id，不该共享
#   custom_phrase.txt  未修改过，与 plum 原版一致
